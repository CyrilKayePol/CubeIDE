fn main()
var a = 0, m = 0
var b = (10+191) * (1 + 90+6)
	while(m < 2)
		print "Yoohoo " + m
		m = m + 1
	end
	print "Yow" + m
	stringPrint("HII", 2)
	print b
	
	if(b < 100)
		a = 200 * 2 + 3
		print a
	elsif(b > 100)
		b = 200
		print "Its a b: "+b
	else
		print m
	end
end

fn stringPrint(var str, var s)
	if(str == s)
		str = "Hello"
	else
		str = "Yaya"
	end
	print "Str is: "+str
end